import open3d as o3d
import torch
import numpy as np
import matplotlib.pyplot as plt
import torch.nn as nn

device = torch.device("cuda:0" if torch.cuda.is_available() else "cpu")

class Pose_box_visualizer:
    def __init__(self, model, test_loader):
        pass

    def visualize(self):
        pass
